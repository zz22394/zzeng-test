version: 0.2

env:
    variables:
        ROOT_DIR : ./z_root_ap
        ARARN_CSR_CRT: arn_zz1
        ENV : test02

phases:
    pre_build:
        commands:
          - echo "zzeng-001-pre_build"
    post_build:
        commands:
          - echo "zzeng-001-post_build"


artifacts:
  files:
    - ./build/zzeng/taskdef_${ENV}.json